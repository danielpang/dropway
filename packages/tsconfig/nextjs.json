{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Base config for the Next.js dashboard (apps/dashboard). Layers DOM libs, JSX, and bundler-friendly settings on top of the shared base. The dashboard owns its own final tsconfig.json and extends this.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "noEmit": true,
    "allowJs": true,
    "incremental": true,
    "module": "ESNext",
    "moduleResolution": "Bundler"
  }
}
